# Final Configuration (Auto-generated by hyperparameter sweep)
# This file will be updated with the best performing hyperparameters

model:
  latent_dim: 16  # Will be updated by hyperparameter sweep
  learning_rate: 0.001  # Will be updated by hyperparameter sweep
  activation_fn: "relu"  # Will be updated by hyperparameter sweep
  batch_size: 128  # Will be updated by hyperparameter sweep
  epochs: 50  # Final training with more epochs
  threshold: 95  # Will be updated by hyperparameter sweep

features:
  strategy: "combined"  # From best features sweep

training:
  validation_split: 0.2
  early_stopping_patience: 10
  reduce_lr_patience: 5
  min_delta: 0.001

wandb:
  project: "fraud-detection-autoencoder"
  entity: null
  tags: ["final_model"] 