# Best Features Configuration (Auto-generated by feature sweep)
# This file will be updated with the best performing feature strategy

model:
  latent_dim: 16
  learning_rate: 0.001
  activation_fn: "relu"
  batch_size: 128
  epochs: 10
  threshold: 95

features:
  strategy: "combined"  # Will be updated by feature sweep

training:
  validation_split: 0.2
  early_stopping_patience: 5
  reduce_lr_patience: 3
  min_delta: 0.001

wandb:
  project: "fraud-detection-autoencoder"
  entity: null
  tags: ["best_features"] 